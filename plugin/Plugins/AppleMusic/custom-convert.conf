# The helper returns decrypted FLAC; curl just pipes it in. $START$ is filled
# with "-G --data-urlencode seek=<seconds>" on a seek, empty otherwise.
apm flc * *
	# RT:{START=-G --data-urlencode seek=%s}
	[curl] -sf --max-time 36000 $URL$ $START$
